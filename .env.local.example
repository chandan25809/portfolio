# Generative AI key for villager Quill (Google AI Studio → API keys).
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Optional model id (no "models/" prefix). Default is set in src/app/api/chat/route.ts.
# If you hit 429 / quota on one slug, switch to another supported id from the vendor docs.
GEMINI_MODEL=gemini-2.5-flash
